You are starting a new work session on the autobody shop project.

1. Read `tasks/todo.md` to see what is currently in progress and what is up next.
2. If something is already "In Progress", resume that task.
3. Otherwise, pick the highest-priority item from "Up Next" (High → Medium → Low).
4. Update `tasks/todo.md`: move the chosen task from its section into "In Progress", replacing "_Nothing currently in progress_".
5. Read any relevant source files before making changes (check CLAUDE.md for key file locations).
6. Implement the task fully — backend and frontend if applicable.
7. When the task is done, update `tasks/todo.md`: move it from "In Progress" to "Completed" and mark it with `[x]`.

Do not ask for confirmation before starting. Just read the todo, pick the task, and begin.
